# Claude Code Instructions

このリポジトリ「johba（ジョーバ）」は、カメレオンJPのスマホブラウザ向け 騎手視点3D競馬ゲームです。
共通ルールは chameleonjp_browser_game_kit を継承しています（以下に転載）。

## 基本方針

- 公開ゲーム本体は、原則としてゲームごとのリポジトリで管理する。
- 公開正本は常に `index.html` とする。
- HTML、CSS、JavaScriptは原則1ファイルにまとめる。
- npm、Node、ビルドツール、フレームワークを勝手に追加しない。
- Codeberg Pagesで公開できる構成を優先する。
- ゲーム仕様を勝手に変えない。
- 不明点は断定せず、未確認または要確認と書く。

## スマホ対応

- iPhone SE級の小さい画面でも遊べることを必須にする。
- 横スクロールを出さない。
- ゲーム中の長押しメニュー、テキスト選択、ダブルタップ拡大、不要なページスクロールを抑える。
- ピンチズームを許可するか止めるかは、ゲームごとに仕様書へ明記する。
- 操作ボタンはスマホで押しやすい大きさにする。

## 画面構成

標準の画面遷移は、ホーム → 名前入力またはカウントダウン → ゲーム → 結果。

ホームには、ゲーム開始、ルール説明、ゲームをシェア、実験場へのリンクを置く。
結果画面には、結果、スコア、ランキング送信状態、もう一度、結果をシェア、ホームへ、実験場へを置く。
ランキング対応ゲームでは、仕様に応じて結果画面下部にランキングを表示する。

## Supabase

- ランキング対応ゲームではSupabase連携を基本にする。
- Publishable keyは公開HTMLへ入れてよい。
- service_role key、DBパスワード、秘密鍵は絶対にクライアント側へ入れない。
- スコア送信は二重送信を防ぐ。
- 送信中、成功、失敗を画面に表示する。
- SQLはファイルではなく、チャットにコピペできる形式で出す。

## 実験場連携

新しいゲームを公開する場合、ゲーム本体だけでなく、Supabase、実験場トップ、詳細ランキングページも確認する。
実験場側に `GAMES` 配列がある場合は、Supabase登録だけでは表示されないため、固定配列への追加も確認する。

## ファイル名

- 公開正本は `index.html`。
- 古いコード参照用の途中版は `index_v2.html`、`index_v3.html` のように名前を変える。
- 古いファイルを上書きして履歴が分からなくなる作業は避ける。

## 作業後の確認

作業後は、仕様との不一致、不具合、古い文言、不要コード、スマホ操作、ランキング送信、二重送信、ゲームバランスを確認する。

---

# johba 固有ルール

- 正式仕様は `docs/game-spec.md`。kit共通ルールと食い違う場合は game-spec.md を優先する。
  - 例外として Three.js の CDN 読み込みを許可する（バージョン固定。現在 `three@0.184.0` を importmap + ES modules で指定）。
- ゲームバランス調整値と Supabase の URL / anon キーは、すべて `index.html` のスクリプト先頭の `CONFIG` 定数ブロックに置き、`docs/balance-notes.md` と対応させる。コード内に数値を散らさない。
- 仕様にない判断をした場合は、`docs/decisions.md` に日付つきで必ず1行残す。
- 大きな改修の前に `index.html` を `index_v2.html`、`index_v3.html` の形式でコピーして残す。
- レースシミュレーション層（物理・AI・展開）は THREE / DOM に依存させない。`index.html` 内の
  `// ==== SIM-CORE-BEGIN ====` 〜 `// ==== SIM-CORE-END ====` マーカー間を node で抽出して
  ヘッドレス検証できる構造を保つ（`tools/sim-harness.mjs` 参照）。
- `index.html` 内は `// ==== SECTION: 名前 ====` 形式のバナーコメントで区切り、後続作業が該当箇所だけ読めるようにする。
- 受け入れ条件は `docs/checklist.md`。フェーズ完了ごとに判定を更新する。
- ピンチズームは禁止（進路変更スワイプと競合するため）。

## モデルの使い分け（subagent運用）

- メインセッション：設計、subagentへの指示書作成、成果物の監査・レビュー。実装難易度が特に高い箇所
  （レースシミュレーション中核、描画ループの性能チューニング、subagentが2回失敗した箇所）のみ直接実装してよい。
- Sonnet subagent（`game-ui-implementer`）：定型的・分量の多い実装。画面UI、CSS、HUD、馬名生成、Supabase連携、決まった仕様の書き起こし。
- Opus subagent（`race-core-implementer`）：難度中〜高の実装。Three.jsシーン構築、AI馬の挙動、スタミナ・速度モデル。
- subagentへの指示書は自己完結させる（仕様書の該当章を要約して渡し、リポジトリ全文を読ませない）。
- メインセッションは成果物を差分中心でレビューし、ファイル全文の読み直しを最小限にする。
