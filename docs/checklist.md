# 受け入れ条件チェックリスト

`docs/game-spec.md` 14章「受け入れ条件（完成の定義）」のチェックリスト。フェーズ完了ごとに判定を更新する。

判定凡例：✅ 達成 / 🔶 部分達成 / ❌ 未達 / ⬜ 未着手 / 要実機 = 実機での最終確認が必要

## 1. 通しプレイ

- [ ] iPhone Safari 相当環境でタイトル→結果画面まで通しでプレイできる（要実機）
- [ ] Android Chrome 相当環境で同上（要実機）
- [x] マウスでも一応操作できる（mousedownフォールバック＋キー操作実装。PC実機確認待ち）

状態：🔶（全フロー実装済・構文/ロジック検証済。実機通し確認のみ未）

## 2. パフォーマンス

- [ ] 12頭立てレース中、ミドルレンジ端末想定で30fps以上（要実機）
- [x] 馬モデルはプリミティブ構成・ジオメトリ/マテリアル共有
- [x] 遠い馬のアニメーション更新頻度を落とす性能ゲーティングがある（距離50m超で1/3、120m超で停止）
- [ ] 影はプレイヤー馬のみ、または擬似影 ※現状は影なし（負荷上限の指定であり必須ではない。実機で視認性に問題があれば擬似影を追加）

状態：🔶（負荷対策は実装済。fps実測のみ未）

## 3. 展開の多様性

- [x] 同じ条件で3回プレイして、ペース（ハイ/ミドル/スロー）が変わり得る（sim検証：30レースでハイ/ミドル/スロー全て出現）
- [x] 同じ条件で3回プレイして、着順が毎回変わる（sim検証：同条件10レースで勝ちゼッケン7〜10種）
- [x] 逃げ馬の頭数によりペースが変わり、ハイペースなら前が終盤バテやすい（AIのハナ争いロジックで実装）

状態：🔶（シミュレーション検証済。実機での体感確認が未）

## 4. 騎乗の影響（強い馬でも負け、弱い馬でも好走）

- [x] 掛かりでスタミナを浪費して負けることがある（sim実装：消費×1.5・操作不能。発生率2〜6/10レース）
- [x] スタミナ切れ（バテ）で失速して負けることがある（sim実装：3段階のバテ＋床速度。ハーネスで後方馬の失速を確認）
- [x] 前が壁で進路を失い負けることがある（sim実装：壁による速度制限＋進路変更ブロック。HUDに「前が壁」表示）
- [x] 弱い馬でも展開がはまれば3着以内に入れる（sim実測：能力下位3頭の馬券圏内が40レース中7回＝17.5%）

状態：🔶（シミュレーション検証済。実機での体感確認が未）

## 5. 操作が結果に効く

- [x] 鞭の有無・タイミングで結果が体感できる差になる（sim検証：AI鞭で勝ちタイム短縮を確認。体感差は実機確認待ち）
- [x] 仕掛け（▲）の使い方で結果が変わる（sim実装：目標速度↑＝消費の2乗増。早仕掛けはバテに直結）
- [x] 抑え（▼）の使い方で結果が変わる（sim実装：温存・回復＋掛かり解消の唯一の手段＋末脚ボーナスの原資）
- [x] 進路変更で前が壁を回避でき、結果が変わる（sim実装：壁の速度制限はレーン移動でのみ回避可能）

状態：🔶（モデル上の効果は検証済。実機での体感確認が未）

## 6. UIサイズ

- [x] 縦持ちで文字・ボタンが操作可能なサイズで表示される（全ボタン44px以上・safe-area対応・短画面用メディアクエリ）
- [x] 横持ちで同上（横持ち推奨レイアウト。オーバーレイはflex中央寄せ＋スクロール可）
- [x] iPhone SE級の画面で横スクロールが出ない（fixed+inset:0構成・モバイルレビューで構造上発生しないことを確認）

状態：🔶（コードレビュー済。実機確認のみ未）

---

## 参考：kit公開前チェック（公開時に実施）

- [ ] game_slug・タイトル・説明文の整合（games登録SQL実行時に確認）
- [x] スコア送信が1回だけ／送信中・成功・失敗の表示（実装済（実機確認待ち））
- [x] service_role key が入っていない（publishable key のみ使用）
- [ ] 実験場トップ・詳細ランキングページへの登録（別リポジトリ。GAMES配列への追加が必要）
- [ ] 手動確認端末の記録
